FROM vm/ubuntu:18.04

RUN curl -sL https://deb.nodesource.com/setup_12.x | bash && \
    apt install nodejs && \
    rm -f /etc/apt/sources.list.d/nodesource.list

WORKDIR /src

RUN pnpm install

COPY . .

RUN pnpm run build
RUN BACKGROUND pnpm run start:prod
EXPOSE WEBSITE http://localhost:8000
